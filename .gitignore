# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
# 빌드 산출물(P2-10 sdist/wheel — CI 에서 생성, 커밋 금지)
dist/
build/

# SAGE runtime temp + override 감사로그(P1-5, 프로젝트 로컬 운영 상태 — 커밋 금지)
.sage/tmp/
.sage/override.jsonl

# editor / OS
.DS_Store
*.swp

# prior bkit/claude local state (not part of SAGE)
.bkit/
.claude/settings.local.json
.claude/settings.json
.claude/hooks/
# generated runtime artifacts (sage generate --write at repo root) — 프레임워크 repo 는 산출물 비보유
.codex/
.mcp.json
